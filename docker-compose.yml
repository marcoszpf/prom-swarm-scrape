services:
  scrape:
    image: prom-swarm-scrape:latest
    ports:
      - target: 8080
        published: 9595
        protocol: tcp
        mode: host
    deploy:
      mode: global
      resources:
        limits:
          memory: 128M
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
